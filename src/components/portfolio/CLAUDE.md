# src/components/portfolio/ — 【预留】作品集专用组件

> **状态：预留目录**，当前为空。

## 规划用途

作品集页面专用、但不适合做成通用 block 的组件。

当前作品集内容通过 `src/blocks/` 下的 block 组件（gallery / featured-project / hero 等）渲染，已能满足需求。当出现作品集专属、且无法抽象为通用 block 的组件时再迁入，例如：

- 项目详情模态弹窗
- 作品筛选/分类器
- 作品集专属的滚动叙事编排

## 约定

- 仅作品集使用的组件放这里；可跨场景复用的能力做成 `blocks/`。
- 业务无关视觉原子放 `ui/`。
