# src/components/resume/ — 【预留】简历专用组件

> **状态：预留目录**，当前为空。

## 规划用途

个人简历页面专用、且不适合做成通用 block 的组件。

当前简历类内容通过 `src/blocks/` 下的 block 组件（experience / skills / about 等）渲染。当出现简历专属、且无法抽象为通用 block 的组件时再迁入，例如：

- 简历导出 / 打印视图
- 时间线筛选
- 教育经历专属展示

## 约定

- 仅简历页面使用的组件放这里；可跨场景复用的能力做成 `blocks/`。
- 业务无关视觉原子放 `ui/`。
