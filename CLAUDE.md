# CLAUDE.md — VBSite 项目指南

> 本文档为 AI 编程助手（Claude Code）提供项目上下文，确保所有开发决策与业务目标对齐。

---

## 📋 项目概述

**VBSite** 是一个面向 C 端/自由职业者的**高动效、高定制化静态网页定制与托管服务**平台。

- **目标用户**：个人、自由职业者、设计师、宠物主等
- **典型场景**：在线简历、作品集、亲子成长集、宠物展示页等
- **核心卖点**：极致的视觉特效/动态效果 + 轻量化交付，区别于传统建站工具的过重或缺乏个性

---

## 🏗️ 技术架构

### 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 页面 | 纯 HTML（自包含单文件） | 每个模板/客户页面都是独立 HTML，无外部依赖文件 |
| 样式 | Tailwind CSS v3 CDN | `cdn.tailwindcss.com`，通过 `tailwind.config` 内联配置自定义色值 |
| 动画 | GSAP 3.12 + ScrollTrigger | 专业级滚动动画、时间轴编排、视差效果 |
| 字体 | Google Fonts | Noto Sans SC + Playfair Display |
| 部署 | Cloudflare Pages | 每个客户一个独立目录，push 即部署 |
| 构建 | Node.js 脚本 | 模板 + 客户 JSON → 压缩单文件 HTML |

### 路由设计

采用多租户静态托管模式，每个客户对应独立的项目目录：

```
https://xxxxx.com/cheesyu     → 客户 cheesyu 的定制页面
https://xxxxx.com/demi        → 客户 demi 的定制页面
```

- 二级目录划分，无需子域名
- 每个目录包含独立的 HTML/CSS/JS 资源

### 数据流

```
用户填写表单（风格/模块/动效/资源上传）
        ↓
飞书多维表格（结构化存储）
        ↓
人工/半自动生成定制页面
        ↓
部署到对应客户目录
```

---

## 🎯 当前阶段：MVP

> **核心目标**：以最低成本快速跑通「采集 → 制作 → 部署 → 收费」的整体商业逻辑。

### 官方首页需求

官方首页需包含以下模块：

1. **项目介绍** — 服务定位、核心优势、适用场景
2. **作品展示（Demo）** — 高动效成品的录屏/预览，视觉冲击力是转化关键
3. **价格费用** — 方案 A（一次性）与方案 B（托管）的定价展示
4. **联系我** — 获客入口（微信/飞书等联系方式）

### MVP 范围约束

- ✅ 官方首页（含上述 4 个模块）
- ✅ 1–2 个 Demo 展示页模板
- ✅ 基础的数据采集流程（飞书表格）
- ❌ 自动化生成流程（进阶阶段）
- ❌ 数据分离渲染（进阶阶段）
- ❌ 用户自助管理后台（进阶阶段）

---

## 💰 商业模式

| 方案 | 模式 | 适用客户 |
|------|------|----------|
| **方案 A** | 一次性收费，交付 HTML 源码包，客户自行托管 | 有技术能力的用户 |
| **方案 B** | 初始制作费 + 按年技术托管费 | 省心用户（推荐） |

---

## 📈 营销与获客

- **抖音/小红书**：发布高动效成品录屏视频，通过视觉冲击力吸引粉丝 → 转化私域
- **闲鱼平台**：卡位「个人建站、定制网页、电子作品集、个性相册」等强意向搜索词

> ⚠️ 官方首页本身就是最佳营销物料，动效质量直接决定转化率。

---

## 🗺️ 进阶演进规划

### 阶段一：MVP（当前）

- 人工根据飞书表格数据制作页面
- 服务条款约束：上线后规定时间内免费微调，后期按次/按模块收费

### 阶段二：数据分离渲染

- 前端页面通过 API 动态读取飞书多维表格数据
- 用户直接在飞书表格内修改内容，网页实时同步
- 无需人工介入修改源码，大幅降低运营成本

---

## 📂 项目结构

> 注：项目已从「HTML 单文件」架构迁移到 **React + Vite + GSAP + React Bits**。每个客户页面由 `users/<客户名>/space.json` 配置驱动，通过 `src/blocks/` 下的 block 组件动态渲染。

```
vbsite/
├── CLAUDE.md                  # 本文件 — AI 项目上下文
├── docs/                      # 面向人的文档（admin 使用指南等，非技术读者）
├── functions/                 # Cloudflare Pages Functions（/api/* 服务端：鉴权/GitHub 提交/资源上传）
├── src/
│   ├── CLAUDE.md              # src 模块总览
│   ├── main.tsx               # 入口（顶部 import '@/lib/gsap' 注册插件）
│   ├── App.tsx                # 路由（单用户/多用户双模式）
│   ├── lib/                   # 基础设施（GSAP 统一注册、spaceSchema、admin API）
│   ├── hooks/                 # 可复用 hook（GSAP 动效、配置加载）
│   ├── types/                 # 全局 TS 类型
│   ├── data/                  # 【预留】静态数据资源
│   ├── pages/                 # 路由页面组件（含 admin/ 管理后台）
│   ├── components/            # 通用组件
│   │   ├── ui/                # 动效/视觉原子组件（GSAP、WebGL）
│   │   ├── shared/            # 跨页面共享业务组件
│   │   ├── landing/           # 【预留】落地页专用组件
│   │   ├── portfolio/         # 【预留】作品集专用组件
│   │   └── resume/            # 【预留】简历专用组件
│   └── blocks/                # 配置驱动的页面区块（hero/gallery/...）
└── users/<客户名>/space.json   # 客户配置（pages + theme + blocks）
```

**路由：**
```
/                       → LandingPage（官网落地页，多用户模式）
/:username              → 用户空间首页（如 /cheesyu）
/:username/:pageId      → 用户空间子页面（如 /cheesyu/products）
/admin                  → 管理后台（登录/客户列表/新建用户）
/admin/:userId/edit     → 可视化编辑器（?token= 临时凭证，无效/过期渲染 404）
/api/*                  → Pages Functions（服务端，密钥不出环境变量）
单用户构建（VITE_BUILD_USER）→ 该用户页面，根路径渲染（不含 /admin）
```

**客户图片资产约定：**
- 图片文件放 `public/users/<客户名>/images/`（不放 `users/`——dev 中间件把 `/users/*` 响应固定为 JSON）
- space.json 中引用写**绝对路径** `/users/<客户名>/images/xxx.jpg`（相对路径在子路径页面会解析错）
- `public/` 在 dev 与 generate 两种模式下行为一致，无需额外拷贝逻辑

> **每个目录都有独立的 `CLAUDE.md`** 说明其职责、文件清单与约定。详见各目录下的 CLAUDE.md。

---

## 📁 目录约定（重要）

1. **每创建一个新目录，必须同时创建该目录的 `CLAUDE.md`**，说明：
   - 该目录的职责与边界（放什么、不放什么）
   - 关键文件清单及各自用途
   - 该目录的编码约定（命名、导入、与其它模块的关系）
2. **预留目录**（暂无文件但已规划用途）也要写 CLAUDE.md，标注「预留」并说明未来规划。
3. 目录职责发生变化时，同步更新对应的 CLAUDE.md，保持文档与代码一致。
4. 根 CLAUDE.md 维护顶层目录索引，子目录 CLAUDE.md 不重复抄写全局内容。

---

## 🔧 开发规范

### 代码风格

- 组件命名采用 PascalCase（如 `HeroSection.astro`）
- 样式使用 Tailwind CSS 为主，自定义动画写在 `<style>` 或独立 CSS 文件中
- 动效代码集中管理，便于跨模板复用

### 提交规范

采用 Conventional Commits：

```
feat: 新增首页 Hero 区域动效
fix: 修复移动端滚动卡顿问题
style: 调整作品展示卡片间距
docs: 更新 README 部署说明
chore: 更新依赖版本
```

### 关键原则

- **性能优先**：静态页面必须秒开，动效不得阻塞首屏渲染
- **移动端适配**：所有页面必须完美适配手机端（核心获客渠道来自抖音/小红书）
- **SEO 友好**：官方首页需做好基础 SEO（标题/描述/OG 标签）
- **视觉优先**：动效质量是核心竞争力，不得为性能牺牲关键视觉效果

---

## ⚠️ 注意事项

- 客户页面内容涉及隐私，**禁止**将客户真实数据提交到公开仓库
- 飞书 API Token 等敏感信息使用环境变量管理，不硬编码
- 每个客户页面是独立目录，修改一个客户页面不应影响其他客户
- 动效实现应考虑 `prefers-reduced-motion` 无障碍设置
- 部署前务必在真实移动设备上测试动效性能

---

## 📚 相关资源

- 飞书多维表格 API 文档：https://open.feishu.cn/document/server-docs/docs/bitable-v1/bitable-overview
- GSAP 动画库：https://gsap.com/
- Tailwind CSS：https://tailwindcss.com/
